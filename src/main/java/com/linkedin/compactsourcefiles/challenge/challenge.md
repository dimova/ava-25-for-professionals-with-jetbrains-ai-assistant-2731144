# Compact source files

1. Create a new compact source file

2. Give it the following main method:
```java
void main() {
}
```

3. Add a new method below, for example:
```java
public void sayHi() {
    IO.println("Hi!");
}
```

4. Invoke the new method from the `main`. 
5. Run the program.